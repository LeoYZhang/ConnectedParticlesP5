import ParticleSim from '../../dist/connected-particles-p5.esm.min.js';

// p: p5 object
const sketch = (p) => {
    p.setup = () => {
        
    };

    p.draw = () => {
        
    };

    p.windowResized = () => {
        
    };
};

new p5(sketch);
